# Minimal Codex CLI config for rented Linux GPU hosts.

model = "gpt-5.5"
model_reasoning_effort = "xhigh"
personality = "pragmatic"
web_search = "live"

approval_policy = "never"
sandbox_mode = "danger-full-access"
notify = ["codex-notify-tmux"]
file_opener = "none"
project_doc_fallback_filenames = ["CLAUDE.md"]
suppress_unstable_features_warning = true
plan_mode_reasoning_effort = "xhigh"

[tui]
status_line = ["model-with-reasoning", "current-dir", "context-usage", "context-window-size", "five-hour-limit", "weekly-limit"]
theme = "ansi"
# Bells reach tmux (window flag) and Ghostty (dock bounce) even over SSH;
# the default "auto" notification method emits OSC 9, which tmux swallows.
notification_method = "bel"

[projects."/"]
trust_level = "trusted"
